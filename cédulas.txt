# Cédulas

valor = int(input("Digite o valor: "))
notas = [200, 100, 50, 20, 10, 5, 1]
notas.sort()
notas.reverse()
numNotas = []
for i in notas:
    numNotas.append(valor/i)
    valor %= i

for i in range(len(notas)):
    print ("Notas de %d: %d" % (notas[i], numNotas[i], ))